 Как всё я это сделал.
Сперва составил таблицы отдельно на листочке (описание объектов базы данных).
 Потом создал их. Каждому дал внутри определённые: поля, имена, ID, количество, дата и т.д.
 Потом перешёл к связям. Что с чем она должна ссылаться. Например, продукты категории ID ссылаются на категорию ID. 
Если спросите зачем все это надо. То, чтобы потом можно было быстро посмотреть и ориентироваться, что с чем должно соединяться.
После того, как я создал все таблицы, все поля, я перехожу к первой задачи и начинаю её решать.
 
	1. Сколько и каких товаров доставлено на указанный склад за указанный период дат
Как всё я это сделал.
Я выбрал все продукты из таблицы продуктов и для каждого продукта посчитал общее количество единиц продукта, проданных на складе в период с 1 по 10 января 2023 года.
Для этого я объединил таблицы receipts и products по их соответствующим идентификаторам, затем добавил условия фильтрации по идентификатору склада и дате продажи.

	2. Сколько и каких товаров забрали (списали) с указанного склада за указанный период дат
Как всё я это сделал.
	Я использовал оператор SELECT для выбора двух столбцов из таблиц их дата одной таблицы (products) и суммы количества единиц из другой таблицы. Далее, я использовал оператор FROM для указания таблиц, из которых выбираются данные и оператор JOIN для объединения этих таблиц по ключу product_id из таблицы issues и id из таблицы products.
Затем, я использовал оператор WHERE для фильтрации только тех строк, где warehouse_id равен 1 и дата выпуска находится в интервале между '2023-01-01' и '2023-01-10'.
После этого, я использовали оператор GROUP BY для группировки результатов по названию продукта. В конце, я использовал оператор ORDER BY для сортировки результатов по названию продукта (product.name) в алфавитном порядке.

	3.Какое количество товаров и каких категорий доставлено на указанный склад за указанный период дат.
Как всё я это сделал.
	В блоке SELECT я выбрал название категории (categorie.name) и использовал функцию SUM для подсчета общего количества продуктов (receipt.quantity).
Затем я объединил таблицы receipts, products и categories через соответствующие ключи (product_id и category_id) с помощью операторов JOIN.
В блоке WHERE я указал условия для выбора только тех строк, где warehouse_id равен 1 и дата receipt находится в заданном диапазоне.
Далее я использовал оператор GROUP BY для группировки результатов по названию категории, чтобы рассчитать общее количество для каждой категории.
В конце, я отсортировал результаты по названию категории (categorie.name) с помощью оператора ORDER BY.

	4.Какое количество и каких брендов товаров доставлено на указанный склад за указанный период дат. 
Как всё я это сделал.
	Для получения данных по названиям брендов и общему количеству продуктов в чеках я использовал запрос на выборку данных с использованием функции COALESCE для замены NULL значений на 'Без бренда'. 
Я объединил таблицы receipts и products по их ID, а затем сделал LEFT JOIN с таблицей brands по ID бренда продукта.
Затем я добавил условия WHERE для выбора данных из склада с ID 1 и за определенный период времени с помощью оператора BETWEEN. 
Затем сгруппировал данные по названию бренда и использовал функцию SUM для вычисления общего количества продуктов в каждом чеке.
В конце, отсортировал данные по названию бренда в алфавитном порядке с помощью оператора ORDER BY.

	5.Количественные остатки товаров на выбранном складе на указанную дату.
Как всё я это сделал.
Для выполнения данного запроса, я использовал следующие таблицы: stock (псевдоним _stock), products (псевдоним product) и receipts (псевдоним receipt). Я соединил таблицы stock и products по полю product_id, а также таблицы stock и receipts по полю receipt_id.
Далее, я установил условия для выборки данных:
warehouse_id равен 1
дата receipt находится в пределах '2023-01-10' и '2023-01-10'
Затем, я отсортировал результаты по наименованию продукта (product.name).
Таким образом, данный запрос выводит наименование продукта и количество товара на складе, соответствующее указанным условиям.

Все остальные(6-11) так же мной были составлены. Но их я уже не описывал.

После всех операций. Сохранил базу данных .



